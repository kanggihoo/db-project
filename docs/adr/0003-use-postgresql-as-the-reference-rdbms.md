# Use PostgreSQL as the Reference RDBMS

This repo uses PostgreSQL as the reference RDBMS for all database optimization and operations experiments. The roadmap depends on PostgreSQL-specific observability, indexing, MVCC, locking, migration, and WAL/CDC behavior, so results are not intended to be database-agnostic.
