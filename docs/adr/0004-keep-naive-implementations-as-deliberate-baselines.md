# Keep Naive Implementations as Deliberate Baselines

Naive implementations in early phases are deliberate experimental controls, not accidental low-quality code. Agents should not optimize them unless the active phase or issue explicitly asks for that optimization, because later phases depend on the naive behavior as measurable baseline evidence.
